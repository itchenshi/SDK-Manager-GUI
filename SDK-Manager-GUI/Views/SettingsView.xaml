<UserControl x:Class="SDK_Manager_GUI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,0,16">
            <TextBlock Text="{DynamicResource Settings_Title}" FontSize="24" FontWeight="Bold" Foreground="#1A1A2E" Margin="0,0,0,16"/>

            <!-- 安装路径 -->
            <Border Background="#FFFFFF" CornerRadius="6" Padding="16" Margin="0,0,0,12"
                    BorderBrush="#E0E0E0" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="{DynamicResource Settings_InstallPath}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding DefaultInstallPath, UpdateSourceTrigger=PropertyChanged}" Padding="6,4"/>
                        <Button Grid.Column="1" Content="{DynamicResource Settings_Browse}" Command="{Binding BrowseInstallPathCommand}"
                                Padding="12,6" Margin="8,0,0,0"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- 下载设置 -->
            <Border Background="#FFFFFF" CornerRadius="6" Padding="16" Margin="0,0,0,12"
                    BorderBrush="#E0E0E0" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="{DynamicResource Settings_DownloadSettings}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <Grid Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{DynamicResource Settings_MaxConcurrent}" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding MaxConcurrentDownloads, UpdateSourceTrigger=PropertyChanged}" Padding="6,4" Width="80"
                                 HorizontalAlignment="Left"/>
                    </Grid>
                    <Grid Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{DynamicResource Settings_MaxRetry}" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding MaxRetryCount, UpdateSourceTrigger=PropertyChanged}" Padding="6,4" Width="80"
                                 HorizontalAlignment="Left"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- 语言设置 -->
            <Border Background="#FFFFFF" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1" Padding="20" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="{DynamicResource Settings_Language}" FontSize="16" FontWeight="Bold" Foreground="#1A1A2E" Margin="0,0,0,4"/>
                    <TextBlock Text="{DynamicResource Settings_LanguageDesc}" FontSize="13" Foreground="#666666" Margin="0,0,0,16"/>
                    <ItemsControl ItemsSource="{Binding Languages}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Command="{Binding DataContext.SelectLanguageCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}"
                                        Cursor="Hand" Margin="0,0,12,0">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="LangCard" Background="#F5F5FA" CornerRadius="8" BorderThickness="2"
                                                    Padding="20,14" BorderBrush="#E0E0E0">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                    <Setter TargetName="LangCard" Property="BorderBrush" Value="#0066CC"/>
                                                    <Setter TargetName="LangCard" Property="Background" Value="#EBF5FF"/>
                                                </DataTrigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="LangCard" Property="Background" Value="#EDEDF5"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                    <StackPanel>
                                        <TextBlock Text="{Binding DisplayName}" FontSize="15" FontWeight="SemiBold" Foreground="#1A1A2E"
                                                   HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="CodeText" Text="{Binding Code}" FontSize="11" Foreground="#999999"
                                                   HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- 操作按钮 -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                <Button Content="{DynamicResource Settings_ResetDefault}" Command="{Binding ResetSettingsCommand}"
                        Padding="16,8" Margin="0,0,8,0"/>
                <Button Content="{DynamicResource Settings_SaveSettings}" Command="{Binding SaveSettingsCommand}"
                        Padding="16,8" Background="#0066CC" Foreground="White"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
